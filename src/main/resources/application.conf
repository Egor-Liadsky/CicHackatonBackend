ktor {
    deployment {
        port = 8084
        port = ${?PORT}
    }
    application {
        modules = [ com.lyadsky.ApplicationKt.module ]
    }
}

jwt {
    secret = "secret"
    issuer = "http://127.0.0.1:8084"
    domain = "http://127.0.0.1:8084"
    audience = "users"
    realm = "ktor news app"
}